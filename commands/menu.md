---
description: Configure Claura ambient audio
allowed-tools: Bash
argument-hint: [status|on|off|mute|set volume N|set sound NAME]
---
Run exactly: `${CLAUDE_PLUGIN_ROOT}/bin/claura-cli.sh $ARGUMENTS`

Output the script's JSON stdout verbatim inside a code block. Do not paraphrase, summarize, or invent subcommands. If `$ARGUMENTS` is empty, run `status`.
